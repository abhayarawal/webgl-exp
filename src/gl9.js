import { animationFrameScheduler, interval, of } from 'rxjs';

var sceneGraph = {
  entities: {
    meshes: {
    }
  }
}

interval(0, animationFrameScheduler)
.pipe(
)
.subscribe(_ => {

})

// mesh - buffer[attrs]; 